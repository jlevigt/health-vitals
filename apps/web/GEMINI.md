# Web Application

## Purpose

React 19 / Vite Frontend application.

## Rules

- API Client: Uses native `fetch` wrapped in `src/api/client.ts`. Does NOT use `axios`.
- Components: Built with Tailwind CSS and Lucide React icons.
- Types: Imports types from `@health-vitals/contracts`.
