# Server Config
PORT=3000
NODE_ENV=development

# Database
DATABASE_URL=postgres://local_user:local_password@localhost:5432/dev
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=local_user
POSTGRES_PASSWORD=local_password
POSTGRES_DB=dev

# Security
SECRET_JWT_KEY=your_super_secret_jwt_key_change_me

# AI Provider (Optional - falls back to Mock if empty)
GEMINI_API_KEY=

# Mail (Optional - defaults to Ethereal for dev)
MAIL_HOST=
MAIL_PORT=
MAIL_USER=
MAIL_PASS=

# Storage (Minio for dev, R2 for prod)
STORAGE_ENDPOINT=http://localhost:9000
STORAGE_REGION=us-east-1
STORAGE_ACCESS_KEY=minioadmin
STORAGE_SECRET_KEY=minioadmin

# RabbitMQ
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
